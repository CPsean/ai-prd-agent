执行 `.claude/skills/ingest-prd.md` 中定义的历史PRD录入流程。

用户已提供原始PRD内容和需求ID。按 skill 定义的 Step 0~8 顺序完整执行，不跳过任何步骤。

关键约束：
- Step 0 必须先读文件，不得依赖记忆中的数据
- 内容缺失时写占位符，不杜撰业务逻辑
- 产品线未标注时必须在 Q1 进行推断确认
- 每次最多输出5个缺口问题，优先影响验收标准和业务规则的
