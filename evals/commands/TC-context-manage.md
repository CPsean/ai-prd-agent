# 测试用例：上下文管理（F-003 产品上下文管理）

> 关联 PRD：`prds/产品上下文管理/prd.md`（F-003）
> 关联文件：`CLAUDE.md`（意图匹配规则）、各 `context/*.md` 文件、命令文件（`new-prd.md`、`requirement-clarifier.md`）
> 前置条件：`context/` 目录已初始化，至少有部分 context 文件存在
> Codex 等效输入：「导入上下文：[内容]」或「帮我录入产品背景」

---

## TC-CTX-01 Import：纯文字导入 → 自动识别内容类型

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-001 |
| **测试目标** | PM 粘贴纯文字内容时，AI 正确识别内容类型和业务含义 |

**测试输入**
```
帮我录入一下产品背景：我们是一个面向中小企业的 SaaS 财务系统，核心功能是报销审批和发票管理。主要用户是财务人员和普通员工。
```

**预期行为**
1. AI 识别内容格式为「纯文字」
2. AI 解析业务含义为「产品背景/定位」
3. 输出识别结果：「识别到这是产品背景描述，业务含义：SaaS 财务系统定位和核心功能。建议归类至 product-background.md」
4. 进入 CTX-IMP-002 分类建议环节

**检查要点**
- [ ] AI 正确识别内容类型为纯文字
- [ ] AI 正确判断归属为 product-background.md
- [ ] 本步骤未写入任何文件

---

## TC-CTX-02 Import：Markdown 文件导入 → 多分类拆分

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-001, CTX-IMP-002 |
| **测试目标** | 导入内容涉及多个 context 文件时，AI 拆分为多条分类建议 |

**测试输入**
```
帮我导入以下内容：

## 用户角色
- 管理员：负责系统配置和审批流程设定
- 普通员工：提交报销单和查看审批进度

## 业务术语
- 报销单：员工提交的费用报销申请
- 审批流：报销单从提交到完成的审批路径
```

**预期行为**
1. AI 识别内容包含两个不同分类
2. 生成分类建议表：

   | 内容摘要 | 建议归类文件 | 建议写入位置 |
   |---------|------------|------------|
   | 用户角色（管理员、普通员工） | user-persona.md | 角色定义章节 |
   | 业务术语（报销单、审批流） | business-glossary.md | 术语表 |

3. 展示分类建议，等待 PM 确认

**检查要点**
- [ ] 内容被正确拆分为 2 条建议
- [ ] 每条建议标注了归属文件和写入位置
- [ ] 本步骤未写入任何文件，等待确认

---

## TC-CTX-03 Import：不支持的格式 → 拒绝并说明

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-001 |
| **测试目标** | PM 上传不支持的格式时，AI 明确拒绝并列出支持格式 |

**测试输入**
```
帮我导入这个 PDF 文件（附 PDF 文件）
```

**预期行为**
1. AI 检测到格式为 PDF
2. 明确拒绝：「当前支持文字、.md 文件、图片截图，PDF 暂不支持」
3. 不进入分类流程

**检查要点**
- [ ] 有明确的格式不支持提示
- [ ] 列出了支持的格式清单（文字、.md、图片截图）
- [ ] 未进入分类建议环节

---

## TC-CTX-04 Import：语义模糊 → 追问 1 个区分问题

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-001 |
| **测试目标** | 内容语义模糊时，AI 追问一个区分问题而非强行分类 |

**测试输入**
```
帮我录入：系统需要支持高并发场景下的数据一致性
```

**预期行为**
1. AI 判断内容语义模糊（可能是产品策略/技术约束/非功能性需求）
2. 追问一个区分问题，如：「这段内容主要描述的是产品策略原则、技术架构约束、还是某个功能的性能要求？」
3. 等待 PM 回答后再确定分类

**检查要点**
- [ ] AI 未强行分类
- [ ] 追问了 1 个区分问题
- [ ] 追问使用了业务语言，易于理解

---

## TC-CTX-05 Import：PM 确认 → 批量写入

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-003 |
| **测试目标** | PM 回复「确认」后，AI 批量写入并输出摘要 |

**测试输入**（接续 TC-CTX-02 的分类建议后）
```
确认
```

**预期行为**
1. AI 将用户角色信息追加到 user-persona.md 对应章节
2. AI 将业务术语追加到 business-glossary.md 术语表
3. 输出写入摘要：「已写入 2 条内容：user-persona.md、business-glossary.md」
4. 追加方式写入，不覆盖已有内容

**检查要点**
- [ ] 内容正确写入对应文件
- [ ] 追加写入，未覆盖已有内容
- [ ] 输出了写入摘要，列出目标文件

---

## TC-CTX-06 Import：PM 修改分类 → 更新后重新确认

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-003 |
| **测试目标** | PM 指出某条分类需修改时，AI 更新后重新展示等待确认 |

**测试输入**（接续 TC-CTX-02 的分类建议后）
```
第一条不是用户画像，应该放到 permission-model.md 的角色定义里
```

**预期行为**
1. AI 更新第一条分类：归属文件改为 permission-model.md
2. 重新展示完整分类建议表（更新后版本）
3. 再次等待 PM 确认

**检查要点**
- [ ] 只更新了 PM 指定的条目
- [ ] 重新展示了完整建议表
- [ ] 未自动写入，继续等待确认

---

## TC-CTX-07 Import：PM 取消 → 不写入

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-003 |
| **测试目标** | PM 回复「取消」时，不写入任何内容 |

**测试输入**（接续分类建议后）
```
取消
```

**预期行为**
1. AI 不写入任何 context 文件
2. 结束导入流程

**检查要点**
- [ ] 未写入任何文件
- [ ] 流程正常结束，无残留操作

---

## TC-CTX-08 Import：目标文件不存在 → 询问是否创建

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-IMP-003 |
| **测试目标** | 目标 context 文件不存在时，询问 PM 是否创建 |

**前置条件**
- `context/permission-model.md` 文件不存在

**测试输入**
```
帮我录入权限规则：管理员可以配置审批流，普通员工只能提交报销
```

**预期行为**
1. AI 识别归类为 permission-model.md
2. 发现文件不存在，询问 PM：「permission-model.md 尚未创建，是否创建该文件并写入？」
3. PM 确认后创建文件并写入

**检查要点**
- [ ] AI 检测到文件不存在
- [ ] 询问 PM 是否创建，未自动创建
- [ ] PM 确认后才创建并写入

---

## TC-CTX-09 截图库：导入截图 → 按模块归类存放

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-SCR-001 |
| **测试目标** | PM 导入页面截图时，AI 按功能模块归类到 context/screenshots/ |

**测试输入**
```
（粘贴一张报销单列表页截图）这是报销管理模块的列表页
```

**预期行为**
1. AI 识别为截图类型
2. 确认功能模块为「报销管理」
3. 建议文件名：报销单列表页.png
4. 写入路径：`context/screenshots/报销管理/报销单列表页.png`
5. 触发 CTX-SCR-002 询问页面跳转关系

**检查要点**
- [ ] 截图存放路径正确（context/screenshots/[模块]/[页面名].png）
- [ ] 模块目录不存在时自动创建
- [ ] 触发了跳转关系维护流程

---

## TC-CTX-10 截图库：跳转关系维护 → page-navigation.md 更新

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-SCR-002 |
| **测试目标** | 截图存放后，AI 询问跳转关系并更新 page-navigation.md |

**测试输入**（接续 TC-CTX-09 后 AI 询问跳转关系）
```
从首页点击「报销管理」菜单进入
```

**预期行为**
1. AI 解析跳转信息：首页 → 报销单列表页，触发方式为点击「报销管理」菜单
2. 更新 page-navigation.md，追加 Mermaid 节点和连线：
   ```
   首页 -->|点击报销管理| 报销单列表页
   ```
3. 展示更新后的局部 Mermaid 图

**检查要点**
- [ ] page-navigation.md 正确追加节点和连线
- [ ] Mermaid 节点 ID 不含空格（用下划线替代）
- [ ] 展示了更新后的图
- [ ] page-navigation.md 不存在时自动创建

---

## TC-CTX-11 按需读取：信号词触发 → 静默读取对应文件

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-RDC-001 |
| **测试目标** | 命令执行中出现信号词时，AI 自动读取对应 context 文件 |

**测试输入**（在 /new-prd 或 /requirement-clarifier 执行中）
```
这个功能需要区分管理员和普通员工的权限
```

**预期行为**
1. AI 识别「管理员」「权限」为信号词
2. 静默读取 `context/permission-model.md`
3. 在后续输出中直接引用权限模型内容（如角色列表、权限规则）
4. 不输出「我读取了 permission-model.md」之类的提示

**检查要点**
- [ ] AI 在输出中引用了 permission-model.md 的内容
- [ ] 未向 PM 暴露读取行为（静默操作）
- [ ] 引用内容准确，来自文件实际内容

---

## TC-CTX-12 按需读取：同一文件只读取一次

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-RDC-001 |
| **测试目标** | 同一对话中多次出现同类信号词，对应文件只读取一次 |

**测试输入**（在同一对话的两轮中）
```
Round 1 - PM: 这个功能只有管理员能操作
Round 2 - PM: 普通员工也需要查看权限
```

**预期行为**
1. Round 1：AI 读取 permission-model.md 并引用
2. Round 2：AI 复用已读取内容，**不重复读取**文件
3. 两轮输出中均能引用权限相关内容

**检查要点**
- [ ] 只触发一次文件读取
- [ ] 后续轮次复用已读取内容

---

## TC-CTX-13 按需读取：文件不存在 → 静默跳过

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-RDC-001 |
| **测试目标** | 信号词触发但 context 文件不存在时，静默跳过不报错 |

**前置条件**
- `context/permission-model.md` 文件不存在

**测试输入**
```
这个功能需要区分管理员和普通员工的权限
```

**预期行为**
1. AI 识别信号词并尝试读取 permission-model.md
2. 文件不存在，静默跳过
3. 继续正常执行命令，不中断
4. 可在关键章节标注「[待补充]」

**检查要点**
- [ ] 未报错或中断命令执行
- [ ] 静默跳过，未向 PM 暴露文件缺失信息（除非触发 CTX-RMD-001 提醒）

---

## TC-CTX-14 缺口提醒：关键 context 文件缺失 → 末尾提醒

| 字段 | 内容 |
|------|------|
| **状态** | — |
| **Codex 状态** | — |
| **功能编号** | CTX-RMD-001 |
| **测试目标** | 关键 context 文件缺失或为空时，AI 在输出末尾追加补充提醒 |

**前置条件**
- `context/product-background.md` 文件为空或不存在
- 执行 /new-prd 命令

**测试输入**
```
/new-prd feature 报销审批
```

**预期行为**
1. AI 正常执行 /new-prd 流程
2. 发现 product-background.md 为空或不存在
3. 在命令输出末尾追加提醒（不中断主流程）：
   > 💡 **上下文补充建议**：发现 product-background.md 尚未初始化。
   > 建议运行 Import & Classify 补充，或直接告诉我相关信息我来帮你写入。
4. 同一对话中同一缺口只提醒一次

**检查要点**
- [ ] 主命令流程正常执行，未被中断
- [ ] 提醒出现在输出末尾
- [ ] 提醒包含缺口文件名和建议操作
- [ ] 同一缺口在同一对话中未重复提醒
