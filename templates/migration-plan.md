# 存量迁移操作路径 — [迁移标题]

> 执行日期：YYYY-MM-DD
> 执行前提：[描述必须先完成的前置条件，如"新命令已合入并验证"]
> 预计耗时：[预估时间]
> 风险等级：[低/中/高]（[一句话说明风险类型]）

---

## 前置检查

- [ ] 确认当前无其他人正在执行相关写操作（避免并发冲突）
- [ ] 确认 git 工作区干净或已提交（`git status` 无未提交变更），便于出问题时回退
- [ ] [补充其他前置检查项]

---

## 第一步：[核心重命名/结构调整]

> 说明：将现有文件/目录从旧格式调整为新格式。

逐行执行，每行是一个操作：

| # | 原路径 | 新路径 | 备注 |
|---|--------|--------|------|
| 1 | `[旧路径1]` | `[新路径1]` | |
| 2 | `[旧路径2]` | `[新路径2]` | |

**执行命令**（可一次性复制执行）：

```bash
cd "[项目根目录]"
mv "[旧路径1]" "[新路径1]"
mv "[旧路径2]" "[新路径2]"
```

**验证**：[描述预期结果，如 `ls [目录]` 应看到什么]

---

## 第二步：[更新索引/注册表]

> 说明：将引用旧路径的索引文件更新为新路径。

| 文件 | 字段/行 | 原值 | 新值 |
|------|---------|------|------|
| `[索引文件路径]` | [ID-1] 路径列 | `[旧路径]` | `[新路径]` |
| `[索引文件路径]` | [ID-2] 路径列 | `[旧路径]` | `[新路径]` |

**验证**：打开索引文件，确认所有路径已更新。

---

## 第三步：[清理冗余/孤立文件]

> 说明：删除迁移后不再需要的历史残留文件。删除前确认每个目录在新位置已有对应副本。

| # | 待删除路径 | 内容 | 对应新位置 | 安全性 |
|---|-----------|------|-----------|--------|
| 1 | `[冗余路径1]` | [内容描述] | `[新位置]` ✅ | 安全删除 |
| 2 | `[冗余路径2]` | [内容描述] | 测试残留，无对应 | 安全删除 |

**执行命令**：

```bash
cd "[项目根目录]"
rm -rf "[冗余路径1]"
rm -rf "[冗余路径2]"
```

**不删除的目录**（如有）：

| 目录 | 原因 |
|------|------|
| `[保留目录]` | [保留原因，如"当前活跃草稿，后续步骤处理"] |

**验证**：`ls [���录]` 应只剩 [预期内容]。

---

## 第四步：[处理活跃/特殊条目]

> 说明：对不能直接删除的活跃条目进行格式调整和注册。

```bash
cd "[项目根目录]"
mv "[旧路径]" "[新路径]"
```

追加注册信息到对应注册表文件（如适用）。

**验证**：
- `[新路径]/[主文件]` 存在
- 注册表包含对应条目

---

## 第五步：[更新内部引用]

> 说明：更新迁移涉及的文件内部相互引用路径。

| 文件 | 字段 | 原值 | 新值 |
|------|------|------|------|
| `[文件路径]` | [字段名] | `[旧引用]` | `[新引用]` |

---

## 第六步：验收检查

全部操作完成后，运行以下命令验证：

| # | 验证操作 | 预期结果 |
|---|---------|---------|
| 1 | `ls [主目录]` | [预期内容] |
| 2 | `ls [辅助目录]` | [预期内容] |
| 3 | 功能冒烟测试 | [关键功能正常工作] |
| 4 | 索引/注册表内容检查 | [所有条目正确] |

---

## 回退方案

如果任何步骤出错：

```bash
git checkout -- [受影响的目录和文件]
```

恢复到操作前状态，排查问题后重新执行。

---

## 使用说明

1. 复制此模板，替换所有 `[占位符]` 为实际内容
2. 根据实际迁移需要增删步骤（不必拘泥于 6 步结构）
3. 每个步骤必须包含：操作说明、执行命令、验证方法
4. 清理步骤中的每个删除项必须标注"对应新位置"和"安全性"
5. 执行前确保 git 工作区干净，以便随时回退
6. 建议逐步执行并验证，不要一次性执行所有步骤
